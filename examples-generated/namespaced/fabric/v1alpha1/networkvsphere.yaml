apiVersion: fabric.vra.m.crossplane.io/v1alpha1
kind: NetworkVsphere
metadata:
  annotations:
    meta.upbound.io/example-id: fabric/v1alpha1/networkvsphere
  labels:
    testing.upbound.io/example-name: simple
  name: simple
  namespace: upbound-system
spec:
  forProvider:
    cidr: ${var.cidr}
    defaultGateway: ${var.gateway}
    domain: ${var.domain}
    tags:
    - key: foo
      value: bar
